modules:
  - type: systemd 
    system:
      enabled:
        - sddm.service

  - type: rpm-ostree
    install:
      - qt5-qtquickcontrols2
      - qt5-qtgraphicaleffects
      - qt5-qtsvg

  - type: script
    scripts:
      - install_sddm_simple_theme.sh