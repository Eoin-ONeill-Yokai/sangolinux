modules:
  - type: systemd 
    system:
      enabled:
        - sddm.service

  # SDDM-Simple dependencies
  - type: rpm-ostree
    install:
      - qt5-qtquickcontrols2
      - qt5-qtgraphicaleffects
      - qt5-qtsvg
      - qt5-qtmultimedia

  - type: script
    scripts:
      - install_sddm_simple_theme.sh