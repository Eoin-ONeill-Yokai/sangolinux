# image will be published to ghcr.io/<user>/<name>
name: maetel
# description will be included in the image's metadata
description: A desktop environment familiar to new users. 

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/ublue-os/base-main
image-version: latest # latest is also supported if you want new updates ASAP

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - from-file: mesa-basics.yml
  - from-file: xorg-compat.yml

  - from-file: cosmic-basics.yml
  - from-file: cosmic-flatpaks.yml
  - from-file: shared-flatpaks.yml

  - from-file: essential-themes.yml

  - from-file: rust-core-and-utils.yml
  
  - type: script
    scripts:
      - cosmic/install_caffeine.sh

  - type: files
    files:
      - source: system/etc
        destination: /etc
      - source: system/maetel # copies config/files/usr into your image's /usr.
                  # put configuration files you want in /etc/ on a booted system 
                  # in /usr/etc/ in the image. read more in files module reference.
        destination: /
      - source: system/graphics/earth_lights_environment_globe-cc0.jpg
        destination: /usr/share/backgrounds/earth_lights_environment_globe-cc0.jpg
      - source: system/usr
        destination: /usr

  - type: os-release
    properties:
      ID: fedora_maetel
      ID_LIKE: fedora
      Name: Maetel
      PRETTY_NAME: Maetel [Fedora] Image
  
  - type: containerfile
    snippets:
      - RUN mv /etc/os-release /etc/os-release.bak
      - RUN sed 's,PRETTY_NAME=.*,PRETTY_NAME="Maetel '$( date +%y.%m.%d )'",' /etc/os-release.bak | tee /etc/os-release
      - RUN rm /etc/os-release.bak

  - type: signing # this sets up the proper policy & signing files for signed images to work fully

