type: dnf
repos:
  cleanup: true
  copr:
    enable:
      - yalter/niri
install:
  packages:
# Basic environment needs
    - xorg-x11-server-Xwayland # X11 compat
    - power-profiles-daemon # For laptop power profile
    - xdg-user-dirs # For common home folders / environment variables, such as ~/Downloads, ~/Documents, etc etc
    - polkit # Security / privledge elevation
    - dbus-daemon
    - dbus-tools # Interaction with dbus daemon
    - vulkan-tools # Basic vulkan testing tools

# CLI Utils
    - bat # A better cat
    - helix # A different text editor, vim substitute. Invoke `hx`.
    - tmux
    - toolbox
    
# Sound
    - wireplumber
    - pipewire
    - pamixer
    - pulseaudio-utils

# Networking
    - NetworkManager-openvpn
    - NetworkManager-openconnect

# Bluetooth
    - bluez
    - bluez-tools

# Extra utilities
    - gnome-disk-utility

# The basics
    - kitty # The terminal
    - ddcutil # Monitor on/off control
    - xfce-polkit # User privledge elevation
    - pavucontrol # Volume control utility
    - wl-clipboard # For wayland clipboard support
    - playerctl # Media player control toolkit
    - vulkan-validation-layers # Validation layer support for development. Might not be necessary?
    - network-manager-applet # Applet for network manager
    - blueman # Applet for bluetooth management

# QT for wayland. Probably just a nice-to-have. 
    - qt5-qtwayland
    - qt6-qtwayland 

# Gnome settings
    - dconf
    - gnome-settings-daemon

# Window manager
    - niri

# File and Management Applications
    - thunar
    - thunar-archive-plugin  # Thunar built-in archive tools
    - thunar-volman # Thunar volume management
    - mousepad # Text editor
    - xarchiver # Archive viewer
    - imv # Image viewer
    - p7zip # Zip util
    - unrar # Rar util
    # GIO framework
    - gvfs-mtp 
    - gvfs-gphoto2
    - gvfs-smb
    - gvfs-nfs
    - gvfs-fuse
    - gvfs-archive
    # Misc
    - android-tools # Allows fast access to android device. Maybe unnecessary in base?

# Display util
    - wlr-randr # Screen config
    - brightnessctl # Brigtness control
    - wlsunset # Sunsetting options
    - kanshi # For screen disabling / auto locking.